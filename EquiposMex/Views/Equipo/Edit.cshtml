@model EquiposMex.Models.EQUIPO

@{
    ViewBag.Title = "Edit";
}

<h2>Edit</h2>

@using (Html.BeginForm()) {
    @Html.AntiForgeryToken()
    @Html.ValidationSummary(true)

    <fieldset>
        <legend>EQUIPO</legend>

        <div class="editor-label">
            @Html.LabelFor(model => model.ID_EQUIPO)
        </div>
        <div class="editor-field">
            @Html.EditorFor(model => model.ID_EQUIPO)
            @Html.ValidationMessageFor(model => model.ID_EQUIPO)
        </div>

        <div class="editor-label">
            @Html.LabelFor(model => model.NOMBREEQUIPO)
        </div>
        <div class="editor-field">
            @Html.EditorFor(model => model.NOMBREEQUIPO)
            @Html.ValidationMessageFor(model => model.NOMBREEQUIPO)
        </div>

        <div class="editor-label">
            @Html.LabelFor(model => model.COLOR)
        </div>
        <div class="editor-field">
            @Html.EditorFor(model => model.COLOR)
            @Html.ValidationMessageFor(model => model.COLOR)
        </div>

        <p>
            <input type="submit" value="Save" />
        </p>
    </fieldset>
}

<div>
    @Html.ActionLink("Back to List", "Index")
</div>

@section Scripts {
    @Scripts.Render("~/bundles/jqueryval")
}
